var condition = false;

if(condition) {
    console.log('Executed!');
}

//En este caso no se ejecuta, ya que normalmente el if funciona siempre que la variable sea verdadera.
//Pero como le estamos diciendo a condition que es false, el if no se ejecuta.